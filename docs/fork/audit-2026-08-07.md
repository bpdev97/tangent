# Tangent upstream sync audit: 2026-08-07

## Result

Tangent merged `upstream/main` at `45d9aa90b` into downstream tip `35e493570`. The merge follows
upstream for shared behavior and retains only the active features registered in
[`FORK.md`](../../FORK.md). The native Swift client branch was not merged.

Upstream's release baseline moved from 0.0.31 to 0.0.32. The sync includes the Agents panel and
workflow observability, paginated thread detail loading, pinned-thread ordering, the modular theme
library, per-environment provider settings, browser-history improvements, remote-update database
snapshots, mobile reconnect and layout fixes, Effect beta.103, and the associated performance and
reliability fixes.

## Tangent compatibility decisions

- Distribution identity remains fork-owned. Desktop state stays under `~/.bpdev-code`, installed
  URL schemes remain `bpdev-code` and `bpdev-code-dev`, Linux registration uses the Tangent desktop
  identity, and releases continue to resolve verified `personal-v*` artifacts from
  `bpdev97/tangent`.
- The upstream updater's protocol-2 database snapshot and restore flow replaces Tangent's older
  migration-blocking preflight. Tangent's verified artifact download, pinned runtime, service
  identity, source sentinel, and bounded archive retention remain intact.
- Upstream now owns migrations 36 through 38. The prior Tangent migration 36 was moved to an
  idempotent migration 39 that also repairs databases whose migration ledger already records the
  old Tangent migration number.
- Generic chats use upstream's shared project-grouping implementation while retaining the reserved
  generic environment, hidden scratch path, locked runtime mode, and disabled filesystem/project
  tools.
- Hermes remains additive to the upstream provider registry and settings architecture. Its gateway
  decoding was updated for Effect beta.103, and its interrupted-input repair now runs through
  migration 39.
- Codex MCP approval support remains additive to upstream's dynamic-tool approval handling.
- Upstream's web Agents surface, inline plan rendering, thread pagination, pin ordering, theme
  system, and provider settings were kept rather than recreating the removed Tangent UI paths.
- Upstream's React Native client changes were merged. The separate native Swift client was
  intentionally excluded.

## Verification

The sync was validated with repository formatting and lint checks, workspace typechecking, mobile
lint, the complete non-server and server test suites under Node 24.13.1, the active fork feature
matrix, production web/server/desktop builds, desktop launch smoke testing, and release-input smoke
testing. Integrated web and mobile client checks and hosted release workflows are recorded in the
release handoff.
