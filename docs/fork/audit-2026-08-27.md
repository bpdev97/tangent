# Tangent upstream sync audit: 2026-08-27

## Result

This sync merges `upstream/main` at `a6797b3b9` into Tangent `main`. The range contains seven
upstream commits after the previous `a3a8cbd60` baseline and advances the server package from
0.0.34 to 0.0.35.

The merge adopts upstream's projection replay, Codex 0.150 compatibility, Grok provider and usage
improvements, stale approval recovery, and release metadata. Every registered fork feature remains
active. The only content conflict was the install guide: Tangent retains its distribution-specific
server wording alongside upstream's new Grok reasoning documentation.

## Upstream changes adopted

- projection bootstrap now replays every unapplied event instead of stopping at the event store's
  default 1,000-event page;
- Codex 0.150 multi-agent activity kinds, collaboration tools, and interrupted statuses are accepted
  across generated app-server schemas;
- stale Codex approval callbacks are normalized without faking an approval resolution;
- Grok gains CLI-owned skill discovery, reasoning-effort options, plan-mode presentation, session
  approval fallback, bounded turn-liveness recovery, and more reliable steering and settlement;
- Grok transcript usage joins Claude and Codex usage, while usage contract v5 remains compatible
  with v4 servers in mixed-version environments;
- macOS preview builds can publish anonymous rolling downloads; and
- upstream package metadata advances to 0.0.35.

## Fork compatibility decisions

- `FORK-DIST-001` retains the `personal-v` release source, `tangent-server` artifacts, Tangent app
  identities, and separate state roots. The upstream Desktop macOS Preview workflow remains
  manually disabled in the Tangent repository.
- `FORK-CHAT-001` keeps the managed generic-chat runtime, cwd, and host-selection rules. Its shared
  provider command reactor adopts upstream's stale Codex approval recovery in an independent hunk.
- Grok improvements remain upstream-owned and are available to generic chats through the provider's
  native behavior; Tangent adds no provider-specific chat policy.
- `FORK-HERMES-001` remains on its supervised TUI-gateway transport. Upstream ACP changes do not
  alter Hermes cursors, profiles, event ordering, or task projection.
- Usage reporting remains an upstream Claude/Codex/Grok contract. Hermes usage is not added as an
  unregistered fork expansion.
- `FORK-PUSH-001`, `FORK-IMAGE-001`, `FORK-PALETTE-001`, `FORK-LINEAR-001`,
  `FORK-MERMAID-001`, and `FORK-PREVIEW-001` have no behavioral overlap with this range.

## Verification

The merged tree passed:

- 339 focused assertions with five intentional skips across Grok, ACP, usage, Codex collaboration,
  projection bootstrap, provider command reactor, generic chat, and personal distribution tests;
- a fresh disposable-database startup through migrations 1 to 44;
- `vp check` with zero errors, `vp run typecheck`, and `vp run lint:mobile`;
- the six-workspace production build (`vp run build`);
- an isolated paired web pass showing the seeded Grok session, cost, provider series, and model row
  on the Usage page; and
- an iOS Simulator pass using the existing Tangent Dev client and the merged Metro bundle, paired to
  the disposable server, showing Grok Build in the mobile 30-day usage chart.

The focused `vp check` run reported existing non-fatal lint warnings, including five inline-schema
compile warnings in upstream's new generated-schema test. Mobile native static checking passed; its
optional SwiftLint, ktlint, and detekt executables were not installed. No native mobile source or
configuration changed in this sync.

XcodeBuildMCP inherited the machine's Xcode beta selection and could not load SimulatorKit for
semantic UI capture. The mobile pass used `/Applications/Xcode.app` explicitly for the same pinned
simulator's pairing, route launch, and screenshots without changing the machine-wide Xcode selection.
